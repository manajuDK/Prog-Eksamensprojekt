<style>
	.UserCreator {
		float: left;
		border: 2px groove grey;
		border-radius: 7px;
		padding: 10px;
		margin: 8px;
	}
	.UserCreatorContent {
		float: right;
		padding: 5px;
	}
</style>

<div class="UserCreator">
	<div class="UserCreatorContent">
		<label for="Name">Name:</label>
		<input type="text" name="Name" autocomplete="false" @onchange="SaveName" />
	</div>
	<br />
	<div class="UserCreatorContent">
		<label for="Balance">Starting Balance:</label>
		<input type="number" name="Balance" autocomplete="false" @onchange="SaveBalance" value=500/>
	</div>
</div>

@code {
	[Parameter]
	public int id { get; set; }

	private void SaveName (ChangeEventArgs e)
	{
		Shared.GlobalInfo.userList[id].name = e.Value?.ToString();
	}
	private void SaveBalance (ChangeEventArgs e)
	{
		try
		{
			Shared.GlobalInfo.userList[id].startBalance = int.Parse(e.Value?.ToString());
		} catch
		{
			
		}
		
	}
}